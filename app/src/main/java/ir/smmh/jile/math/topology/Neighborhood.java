package ir.smmh.jile.math.topology;

import ir.smmh.jile.math.geometry.Point;
import ir.smmh.jile.math.settheory.Set;

/**
 * A {@link Neighborhood} of a {@link Point} is a {@link Set} of points
 * containing that point where one can move some amount in any direction away
 * from that point without leaving the set.
 */
public interface Neighborhood {

}
